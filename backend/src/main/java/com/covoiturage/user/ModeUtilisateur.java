package com.covoiturage.user;

/** Mode actif d'un compte. Un passager simple reste en PASSAGER ;
 *  un conducteur peut basculer entre CONDUCTEUR et PASSAGER. */
public enum ModeUtilisateur {
    PASSAGER,
    CONDUCTEUR
}
